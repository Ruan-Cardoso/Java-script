// node for.js

const numeros = [100, 200, 300, 400, 500, 600];
for (let indice = 0; indice < numeros.length; indice++){
console.log(numeros[indice])
}



const mane = ['berserk', 'jazz', 'fedorenta', 'palmas', 'espinhenta', 'kelly']

for (let pegar = 0;pegar < mane.length; pegar++){
console.log(mane[pegar])

}